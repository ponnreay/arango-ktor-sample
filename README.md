# ArangoDB example using Ktor Framework

```bash
git clone https://github.com/ponnreay/arango-ktor-sample.git

# run it
./gradlew clean run

# jar file

./gradlew clean build
java -jar ./build/libs/arango-ktor-sample.jar
```
